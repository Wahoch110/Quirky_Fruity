/**
 * Legacy static list — the live app now loads products from MongoDB via the API.
 * Kept for reference; Home.jsx and ProductDetail.jsx use fetch() instead.
 */
export const PRODUCTS = [];

export function getProductById() {
  return undefined;
}
